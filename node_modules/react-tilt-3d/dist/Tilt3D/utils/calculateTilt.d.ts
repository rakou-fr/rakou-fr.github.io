import { CalculateTiltOptions, Coords, CursorPos } from '../types';

export declare const calculateTiltOnAxis: (axis: 'x' | 'y', position: number, domNode: HTMLElement, options: Required<Pick<CalculateTiltOptions, 'maxTilt' | 'offset'>> & {
    lockAxis: boolean;
}) => {
    degrees: number;
    exceedsLimit: boolean;
};
export declare const getDistanceFromEdge: (elem: HTMLElement) => Coords;
declare const calculateTilt: (cursorPos: CursorPos, domNode: HTMLElement, options: CalculateTiltOptions) => {
    x: number;
    y: number;
};
export default calculateTilt;
