import { CubicBezier, Tilt, TransitionTimingFunction } from '../types';

export declare const BOUNCE_TRANISTION: CubicBezier;
type GenerateStyleParams = {
    tilt: Tilt;
    maxTilt: number;
    zoomOnTilt?: boolean;
    zoomScale?: number;
    transition?: TransitionTimingFunction;
    gyroMode?: boolean;
    enableLighting?: boolean;
};
declare const generateStyleText: (params: GenerateStyleParams) => string;
export default generateStyleText;
